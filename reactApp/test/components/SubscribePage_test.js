import { renderComponent, expect } from '../test_helper';
import App from '../../src/components/app';

describe('Subscribe Form', () => {
    let component;

    beforeEach(() => {
        component = renderComponent(App);
    });

    it('shows User Form', () => {
    });
    it('shows Submit Form button', () => {
    });
    it('shows empty Email message', () => {
    });
    it('shows empty Invalid Email message', () => {
    });
    it('shows empty First name message', () => {
    });
    it('shows empty Last name message', () => {
    });
    it('shows empty Invalid mobile message', () => {
    });
    it('shows Thank you page', () => {
    });
});









